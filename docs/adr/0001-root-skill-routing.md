# ADR 0001: Preserve a root Skill router

The repository keeps `SKILL.md` as the broad, backward-compatible entrypoint. It routes to specialist Skills rather than duplicating their research methods. This preserves discovery for existing installations while keeping each method independently maintainable.
